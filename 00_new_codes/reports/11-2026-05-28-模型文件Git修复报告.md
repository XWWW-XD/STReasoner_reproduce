# 11-2026-05-28 模型文件 Git 修复报告

## 背景

本次修复目标是处理误进入 Git 的本地模型加载文件，避免 `base_model/STReasoner-8B/tokenizer.json` 这类推理依赖文件继续被提交或被 LFS 指针覆盖。

## 已处理

- 保留 `00_new_codes/repro_autodl/experiments/stage2_subsets/paper_cases/` 下的 paper case 数据文件，并纳入本次提交。
- 对 `base_model/STReasoner-8B/tokenizer.json` 使用 `git rm --cached` 仅从 Git 索引移除；本地文件仍保留，推理加载不受影响。
- 在 `.gitignore` 中新增 `base_model/`，防止后续本地模型目录再次被误加入 Git。
- 在 `.gitignore` 中新增常见模型权重与缓存规则，包括 `*.safetensors.index.json`、`*.bin`、`*.pt`、`*.pth`、`*.ckpt`、`*.gguf`、`*.onnx`、`.cache/`、`model_cache/`、`hf_cache/`、`huggingface_cache/`、`transformers_cache/` 和 `**/models--*/`。
- 修复 `paper_cases_manifest.json` 中缺失的逗号，提交前已通过 JSON/JSONL 基础解析检查。

## 说明

- AutoDL 服务器仅做推理、不训练模型时，`base_model/`、模型权重、tokenizer、本地 HuggingFace cache 都属于环境依赖或运行缓存，通常不应提交到 Git。
- 本次没有删除本地模型文件，也没有 force push。
- 仓库历史中仍存在一些已经被跟踪的 `base_model/` 配置类文件；本次按当前修复目标只移除 `tokenizer.json` 的 Git 跟踪，并通过 `.gitignore` 阻止后续新增模型/cache 文件。
