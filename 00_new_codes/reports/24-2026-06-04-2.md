# 2026-06-04 日志 2

17:38：继续 Stage 1 目标，先核对 `20` 执行计划、`22` 报告和当前输出目录。

17:40：补做 Step 7 最小可用性检查，新增 `single_a100_qwen3_4b_stage1_adapter_load_check.py`，用于验证 base model + LoRA adapter 是否能标准回读。

17:40：第一次加载 `100` step adapter 失败。base model 能加载为 `Qwen3TSForCausalLM`，`ts_encoder` 也存在，但 `PeftModel.from_pretrained` 报 `KeyError: base_model.model.ts_encoder.mlp.0.lora_A.default.weight`。

17:41：检查 `adapter_model.bin` 和 `adapter_config.json`，发现旧脚本同时用了 `--additional_target ts_encoder` 和 `ts_encoder.mlp.*` LoRA target，导致保存出的 key 同时包含 `ts_encoder.modules_to_save.*` 和 `ts_encoder.mlp.*`。

17:42：判断旧版 adapter 训练有效但标准加载不合格，不能作为最终可复用产物。

17:42：修改 `single_a100_qwen3_4b_stage1_lora_smoke.sh` 和 `single_a100_qwen3_4b_stage1_lora_100steps.sh`，删除 `--additional_target ts_encoder`，保留显式 Qwen LoRA target，并让 time-series patch 自动追加 `ts_encoder.mlp.0/2/4/6/8`。

17:42：重跑 10-step smoke。日志显示使用 FlashAttention-2，trainable params 变为 `16,700,480`，符合“不保存整个 ts_encoder，只训练 LoRA”的预期。

17:43：10-step smoke 完成，`train_runtime=52.1041` 秒，`train_loss=16.393671607971193`。

17:43：对修正后的 smoke adapter 做标准加载检查，`PeftModelForCausalLM` 加载成功，`nonzero_lora_B=257/257`，结果为 `adapter_load_check=ok`。

17:44：重跑 100-step 小步数训练，继续使用 FlashAttention-2、bf16、显式 Qwen LoRA target、自动追加 `ts_encoder.mlp.*`。

17:52：100-step 训练完成，`train_runtime=482.5574` 秒，`train_loss=12.218571281433105`。

17:53：对修正后的 100-step adapter 做标准加载检查，`PeftModelForCausalLM` 加载成功，`nonzero_lora_B=257/257`，结果为 `adapter_load_check=ok`。

17:54：检查最终输出目录大小：smoke 和 100-step 均约 `159M`，`adapter_model.bin` 约 `64M`；比旧版 `additional_target=ts_encoder` 的 `365M` 输出更干净。

17:55：更新 `22-Stage1单卡4B实验.md`，把最终指标、日志路径、加载检查和旧版失败原因写进去。

17:56：更新 `20-Stage1正式训练计划.md`，删除旧的 `lora_target=all` / `additional_target=ts_encoder` 建议，改成当前已验证路线。

17:57：更新 `00_new_codes/guides/修改文件必读规则.md`，补充 Stage 1 LoRA 不要使用 `additional_target=ts_encoder`，并要求训练后做标准 adapter 加载检查。
