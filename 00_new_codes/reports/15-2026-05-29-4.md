# 2026-05-29 日志 4

14:36：用户明确指出 `max_new_tokens` 必须是 `6144`，不能使用其他值。复查后确认之前 ST-Test 结果不能作为正式结果。

14:37：检查 `inference/inference_tsmllm_vllm.py` 和 `inference/llm_utils.py`，发现 CLI `--max_tokens` 虽然构造了 `SamplingParams`，但 `worker_vllm_ts()` 实际调用 `llm.generate()` 时仍使用 worker 内部默认参数。

14:38：修改 `inference/llm_utils.py`，让 `worker_vllm()` 和 `worker_vllm_ts()` 优先使用队列中传入的 `SamplingParams`。该修改不改 prompt、不改 gold、不改数据。

14:38：运行 `reasoning_entity` 1 条 `max_tokens=6144` smoke，输出到 `exp/sttest_smoke_entity_6144/generated_answer.json`。gold 为 `<answer>A</answer>`，raw response 为 `<answer>A</answer>`，当前 evaluate accuracy 为 `1.0`。

14:43：完整运行 `reasoning_etiological`，`max_tokens=6144`，输出到 `exp/sttest_full_etiological_6144/generated_answer.json`。当前 evaluate：`207/207`，coverage `1.0`，accuracy `0.956522`。

14:53：完整运行 `reasoning_forecasting`，`max_tokens=6144`，输出到 `exp/sttest_full_forecasting_6144/generated_answer.json`。当前 evaluate：`280/280`，coverage `1.0`，MAE `68.317056`，MAPE `123.289149%`。

15:27：完整运行 `reasoning_entity`，`max_tokens=6144`，输出到 `exp/sttest_full_entity_6144/generated_answer.json`。当前 evaluate：`1194/1194`，coverage `1.0`，accuracy `0.747906`。

16:28：完整运行 `reasoning_correlation`，`max_tokens=6144`，输出到 `exp/sttest_full_correlation_6144/generated_answer.json`。当前 evaluate：`1592/1592`，coverage `1.0`，accuracy `0.831658`。

16:29：导出完整 ST-Test 6144 逐条 raw/gold 对齐证据到 `00_new_codes/reports/artifacts/sttest_full_6144_outputs_with_gold.jsonl`，共 `3273` 行。另写汇总 `sttest_full_6144_summary.json` 和前三条预览 `sttest_full_6144_first3_preview.json`。

16:32：更新 `00_new_codes/reports/11_2.2paper_cases重试修复报告.md`，把 512 结果降级为参数不合规预跑，新增 `max_tokens=6144` 正式结果、修改原因、修改细节、逐条输出/gold 附件路径和仍未解决问题。
