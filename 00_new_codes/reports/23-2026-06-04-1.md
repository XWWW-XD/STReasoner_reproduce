# 2026-06-04 日志 1

10:29：检查单卡 A100 小模型训练环境，确认使用 `/root/autodl-tmp/conda/envs/str-py310/bin/python`，不使用默认 Python。

10:30：检查 4B 模型和 ST-Bench 训练数据，确认 `base_model/Qwen3-4B-Instruct-2507/` 存在，Stage 1 数据 `data/ST-Bench/ST-Align/alignment_train.jsonl` 存在。

10:33：尝试 Stage 1 full fine-tuning smoke，使用官方 `src/train.py` 和 STReasoner-Align 模板。

10:36：第一次 full smoke 卡在依赖，发现 `bitsandbytes==0.43.1` 与当前 `triton==3.2.0` 不兼容，报 `No module named 'triton.ops'`。

11:25：将当前环境中的 bitsandbytes 调整为 `0.45.2` 后重试，确认 torch、triton、deepspeed、transformers 等主依赖不大动。

11:27：发现 DeepSpeed CPUAdam 编译需要 `ninja`，但脚本绝对路径调用 Python 时没有带 conda env `bin`，因此在训练脚本里补 `PATH=/root/autodl-tmp/conda/envs/str-py310/bin:$PATH`。

11:30：CPUAdam 已能编译到 `/root/autodl-tmp/cache/torch_extensions`，没有继续写系统盘。

11:32：full fine-tuning 仍失败。日志显示 CPU virtual memory 到约 `78.48GB`，容器 cgroup 内存上限约 `77.3GB`，进程被 `SIGKILL`，返回码 `-9`。

11:40：判断 full fine-tuning 当前不适合继续重复。失败点不是数据、模型、GPU 显存或系统盘，而是容器内存限制。

11:50：改写 Stage 1 LoRA smoke 脚本，目标是先让单卡训练真实进入 step 并保存 adapter。

11:50：第一次 LoRA smoke 使用 `lora_target=all` 失败。PEFT 报 `Target module Qwen3DecoderLayer(...) is not supported`。

11:51：分析原因是 `ts_encoder.mlp` 的数字子层名被 `lora_target=all` 扩大匹配，误匹配到 Qwen decoder layer。

11:51：改为显式指定 Qwen 线性层：`q_proj,k_proj,v_proj,o_proj,gate_proj,up_proj,down_proj`，再由 time-series patch 添加 `ts_encoder.mlp.*`。

11:51：LoRA retry 进入训练前，但 W&B 要 API key，报 `No API key configured`。

11:52：在脚本里加入 `WANDB_DISABLED=true` 和 `--report_to none`。

11:52：fp16 LoRA smoke 跑完 10 steps，但日志中 `grad_norm=nan`，学习率一直为 0，后续检查发现 LoRA B 仍为零，判断为无效训练。

11:56：改用 A100 更合适的 `bf16`，重新跑 smoke。bf16 后 loss 和 grad norm 正常，adapter 发生更新。

11:58：发现 bf16 成功版本日志显示 `Using torch SDPA`，不符合之前“不许用 sdpa”的要求。

12:07：确认 `flash_attn` 可用，在最终脚本中显式加入 `--flash_attn fa2`。

12:08：启动 Stage 1 LoRA `100` steps 小步数训练，输出到 `output/single_a100_qwen3_4b_stage1_lora_100steps/`。

12:16：`100` steps 小步数训练完成。日志显示使用 FlashAttention-2，`train_runtime=483.0344` 秒，`train_loss=12.430806879997254`。

12:16：启动最终 `10` step smoke rerun，保证 smoke 产物也使用 FlashAttention-2。

12:18：最终 `10` step smoke 完成。日志显示使用 FlashAttention-2，`train_runtime=51.4846` 秒，`train_loss=16.379451560974122`。

12:20：检查训练结束状态，没有训练残留进程。系统盘 `/` 使用率约 `10%`，数据盘 `/root/autodl-tmp` 使用率约 `65%`，剩余约 `18G`。

12:22：检查两个最终 adapter 权重，`lora_B` 张量均为非零，`ts_encoder` 相关张量也均为非零，确认训练不是假成功。

12:25：将脚本设为可执行，并开始整理正式报告、过程日志和经验记录。
