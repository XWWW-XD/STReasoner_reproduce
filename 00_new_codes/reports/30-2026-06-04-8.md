# 2026-06-04-8 Stage 1 2000 save_state 收尾日志

## 本轮目标

本轮只完成 `1500 -> 2000 save_state` 后的验证和断点记录，不再启动新的训练。

## 已完成

- `output/single_a100_qwen3_4b_stage1_lora_2000steps_from1500_save_state/` 已完成 500 steps 训练。
- 训练用时 `2509.1812` 秒，约 `41 分 49 秒`。
- `train_loss = 0.4133506908416748`。
- 输出目录大小约 `287M`。
- `checkpoint-500` 中已确认存在 `optimizer.pt`、`scheduler.pt`、`rng_state.pth`、`trainer_state.json`。
- 标准 PEFT adapter 加载通过：`adapter_load_check=ok`，`nonzero_lora_B=257/257`。
- 30 条健康探针：`17/30`，推理显存峰值约 `8.50GB allocated / 8.60GB reserved`。
- 40 条 temporal balanced 探针：`6/40`，推理显存峰值约 `8.67GB allocated / 8.77GB reserved`。

## 重要解释

之前 `500 -> 1000 -> 1500` 是“加载 adapter 后分段再训”，不是严格 Trainer resume，因为旧 checkpoint 没有 optimizer/scheduler/rng state。

这次 `2000steps_from1500_save_state` 仍然是从 1500 adapter 开始的一段新训练，但它保存了完整 Trainer 状态。因此下一次可以从：

```text
output/single_a100_qwen3_4b_stage1_lora_2000steps_from1500_save_state/checkpoint-500
```

做更严格的 `resume_from_checkpoint`。

## 探针结论

30 条健康探针从 `1500steps_from1000` 的 `16/30` 小幅到 `17/30`。空间类较稳，部分空间-时间类也能答对。

40 条 temporal balanced 仍是 `6/40`，分项为：

```text
amplitude: 0/5
frequency: 0/5
phase: 0/5
evolution pattern: 4/5
long-term baseline: 0/5
kappa: 2/5
sigma: 0/5
lambda: 0/5
```

这说明 loss 继续下降，但 temporal 数值参数还没有明显突破。后续不能只看 train loss，应继续固定使用 30 条健康探针和 40 条 temporal balanced 探针。

## 下次继续入口

优先脚本：

```text
00_new_codes/repro_autodl/experiments/scripts/single_a100_qwen3_4b_stage1_lora_resume_2000to2500_save_state.sh
```

启动前检查这些文件仍存在：

```text
output/single_a100_qwen3_4b_stage1_lora_2000steps_from1500_save_state/checkpoint-500/optimizer.pt
output/single_a100_qwen3_4b_stage1_lora_2000steps_from1500_save_state/checkpoint-500/scheduler.pt
output/single_a100_qwen3_4b_stage1_lora_2000steps_from1500_save_state/checkpoint-500/rng_state.pth
output/single_a100_qwen3_4b_stage1_lora_2000steps_from1500_save_state/checkpoint-500/trainer_state.json
```

今晚断点：不再启动 `2000 -> 2500`，先停。

## 收尾资源检查

本轮结束时没有继续训练进程，GPU 已空闲：

```text
A100-PCIE-40GB: 1MiB / 40960MiB
GPU-Util: 0%
Processes: No running processes found
```

磁盘状态：

```text
/               30G  3.1G   27G  11%
/root/autodl-tmp 50G   33G   18G  66%
```

checkpoint 断点文件再次确认存在：

```text
output/single_a100_qwen3_4b_stage1_lora_2000steps_from1500_save_state/checkpoint-500/optimizer.pt
output/single_a100_qwen3_4b_stage1_lora_2000steps_from1500_save_state/checkpoint-500/scheduler.pt
output/single_a100_qwen3_4b_stage1_lora_2000steps_from1500_save_state/checkpoint-500/rng_state.pth
output/single_a100_qwen3_4b_stage1_lora_2000steps_from1500_save_state/checkpoint-500/trainer_state.json
```

`git status --short` 显示本轮有源码、脚本、报告、日志、结果文件变动，也有之前 checkpoint 清理和 `base_model/STReasoner-8B` 删除状态。这里没有做提交，也没有恢复旧删除状态。

## 明确暂停边界

按用户新要求：不再启动新的训练；但验证、整理、报告、日志、资源检查这些小任务已经继续做完。当前适合关机/下班，明天从 `resume_2000to2500_save_state` 前置检查开始。

