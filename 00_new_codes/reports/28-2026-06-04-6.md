# 2026-06-04 日志 6

19:42：基于 `1000steps_from500` 的 30 条健康检查，决定继续做一段 Stage 1 adapter 接续训练，目标是观察 temporal 是否随更多 step 改善。

19:43：新增脚本：

```text
00_new_codes/repro_autodl/experiments/scripts/single_a100_qwen3_4b_stage1_lora_continue_1000to1500.sh
```

配置：

```text
adapter_name_or_path=output/single_a100_qwen3_4b_stage1_lora_1000steps_from500
output_dir=output/single_a100_qwen3_4b_stage1_lora_1500steps_from1000
max_steps=500
flash_attn=fa2
bf16=true
```

19:43：启动 `1000 -> 1500` 接续训练，日志：

```text
00_new_codes/repro_autodl/experiments/logs/stage1_lora_continue_1000to1500_fa2_20260604_194332.log
```

启动日志确认：

```text
Loaded adapter(s): ./output/single_a100_qwen3_4b_stage1_lora_1000steps_from500
Using FlashAttention-2 for faster training and inference.
Applied SFT learning rate 0.000010 to 10 time-series encoder parameter(s).
```

19:48：训练约 `66/500`，速度约 `4.8` 秒/step，正常。

19:53：训练约 `130/500`。

19:58：训练约 `193/500`。

20:03：训练约 `258/500`。

20:08：训练约 `323/500`。

20:13：训练约 `389/500`。

20:18：训练约 `453/500`。

20:23：训练完成。关键指标：

```text
train_runtime=2391.6142
train_loss=0.5175953221321106
train_samples_per_second=1.673
train_steps_per_second=0.209
loss_first=0.6012
loss_last=0.4339
loss_min=0.2798
loss_max=0.8553
```

20:24：标准 adapter 加载检查通过：

```text
00_new_codes/repro_autodl/experiments/logs/stage1_lora_1500steps_from1000_adapter_load_check_20260604_202407.log
nonzero_lora_B=257/257
adapter_load_check=ok
```

20:24：运行固定 30 条健康检查，产物：

```text
00_new_codes/repro_autodl/experiments/logs/stage1_lora_1500steps_from1000_adapter_probe30_20260604_202435.log
00_new_codes/repro_autodl/experiments/results/stage1_lora_1500steps_from1000_probe30_20260604_202435.jsonl
```

结果：

```text
temporal=1/10
spatial=10/10
spatial_temporal=5/10
total=16/30
```

和上一段对比：

```text
1000steps_from500: temporal=0/10, spatial=10/10, spatial_temporal=4/10, total=14/30
1500steps_from1000: temporal=1/10, spatial=10/10, spatial_temporal=5/10, total=16/30
```

判断：

- 继续训练仍有改善，但幅度变小。
- `spatial` 已经很稳。
- `spatial_temporal` 小幅改善。
- `temporal` 仍是最明显短板，数值类答案经常输出相近但错误的值，例如 `20.0`、`0.0364`、`-1.5708`。
- 下一步如果继续，建议不要只机械加到 2000，而是先分析 temporal 训练样例和输出格式，确认是否需要更多步数、不同采样范围、或更接近官方训练阶段的设置。

20:27：更新 `20-Stage1正式训练计划.md` 和 `22-Stage1单卡4B实验.md`，写入 `1500steps_from1000` 训练、加载、30 条探针和当前判断。
