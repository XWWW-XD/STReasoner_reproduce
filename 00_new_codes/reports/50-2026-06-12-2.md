# 2026-06-12 日志 2 — VSCode/Codex 远端配置（2 服务器）

日期：2026-06-12

## 结论

已按 `49-VSCode远程与Codex稳定连接配置迁移清单.md` 在本机（2 服务器 / A800）写完**远端**配置；**本地 Windows SSH + Remote-SSH 设置需你在本机改**。连接前远端 `127.0.0.1:17997` 无监听是正常的，需 SSH 把本地代理转发到该端口。

## 对话阶段日志

10:25：用户要求读 49 报告，用 superpowers 配 VSC/Codex，配好后用户自行连测。

10:28：通读 49、`05-Codex重连修复.md`；核对本机已有旧机拷贝的 `server-env-setup`、`User/settings.json`、`codex-vscode-wrapper.sh`（端口 17997）。

10:30：探测本机 `17997/7897` 等端口均 `Connection refused`——尚未从 Windows 建立 SSH 转发，符合 49 §2.3 预期。

10:32：补写缺失项：`Machine/settings.json`、统一 wrapper、`bashrc` 代理与 `PS1=[autodl-2]`、检查脚本 `/root/check-vsc-codex.sh`；仓库内 Windows 片段见 `00_new_codes/repro_autodl/vsc_codex_remote_setup/`。

## 本机已改动的远端文件

| 路径 | 作用 |
| --- | --- |
| `/root/.vscode-server/server-env-setup` | VS Code Server 进程继承代理 |
| `/root/.vscode-server/data/User/settings.json` | `chatgpt.cliExecutable` + `http.proxy` |
| `/root/.vscode-server/data/Machine/settings.json` | 同上（此前缺失） |
| `/root/.codex/codex-vscode-wrapper.sh` | Codex CLI 走代理再 exec 扩展内 codex |
| `/root/.bashrc` | 终端代理 + `[autodl-2]` 提示符 |
| `/root/check-vsc-codex.sh` | 49 §8 一键检查 |

代理端口统一 **17997**（与 1 服务器惯例一致；两台机器各自 localhost 不冲突）。

## 用户本机待做（49 §2.1 / §2.2）

1. SSH config：建议 `Host autodl-2` + keepalive + **LocalForward 17997 → 本地代理端口**（snippet 里已注释示例）。
2. VS Code User settings：粘贴 `windows-local-settings.json` 四条 Remote-SSH 项。
3. Remote-SSH 连接 `autodl-2` 后执行：`Remote-SSH: Kill VS Code Server on Host...` 再重连。
4. 远端跑：`bash /root/check-vsc-codex.sh`；`curl` 应能 tunnel，`extensionHost`/`codex app-server` 应有 `HTTP_PROXY`。
5. Codex **重新登录**（不复制旧机 `auth.json`）。

## 用户确认（2026-06-12 补充）

- Windows 本地代理：**7897** ✓
- 本实例 SSH：`connect.nma1.seetacloud.com:48192`（与容器 `44514f8f55` / nm-A1 一致）
- VS Code SSH Host：**autodl-A800**（GPU 为 A800 80GB，非 A100）
- 用户粘贴的 SSH config **未含 LocalForward**；已在 `windows-ssh-config.snippet` 写明 `LocalForward 17997 127.0.0.1:7897`

修改：2026-06-12
