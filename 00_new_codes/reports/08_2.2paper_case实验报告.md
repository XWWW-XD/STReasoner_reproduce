## 2026-05-28-1

### 本轮结论

本轮没有继续正式跑完整 paper_cases。按“如果卡在模型加载、路径、依赖、cache 等问题，请停止并汇报”的要求，smoke test 停在模型权重加载 / HuggingFace cache 获取阶段。

已经确认：

- `git pull --ff-only` 结果为 `Already up to date`，当前 `autodl` 分支已是远端最新。
- 不能使用默认 `/root/miniconda3/bin/python`，因为它是 Python 3.12，且环境为 `torch==2.5.1+cu124`，不符合仓库 `requirements.txt` 中的 Python 3.10 + torch 2.6 + flash-attn wheel 组合。
- 已改用已有环境 `/root/autodl-tmp/conda/envs/str-py310/bin/python`，该环境更接近 `requirements.txt`：
  - Python `3.10.20`
  - torch `2.6.0+cu124`
  - flash_attn `2.7.2.post1`
  - `flash_attn_2_cuda` 可正常 import
- 数据读取链路正常：`list` 能列出 4 条 paper case。
- parser import 正常：`evaluation/evaluate_qa.py` 中 `_extract_tag_content`、`_normalize_choice`、`_parse_series` 均可调用。
- 默认 `flash_attention_2` 前置检查通过，没有切到 `sdpa`。

未完成：

- 模型权重没有成功加载完成。
- `generate` 没有启动。
- prediction / summary 没有生成。
- parser 没有在真实模型输出上运行。

### 执行过的关键命令

```bash
git pull --ff-only

/root/autodl-tmp/conda/envs/str-py310/bin/python -m py_compile \
  00_new_codes/repro_autodl/experiments/scripts/stage2_2_script/run_paper_cases.py

/root/autodl-tmp/conda/envs/str-py310/bin/python \
  00_new_codes/repro_autodl/experiments/scripts/stage2_2_script/run_paper_cases.py list

/root/autodl-tmp/conda/envs/str-py310/bin/python \
  00_new_codes/repro_autodl/experiments/scripts/stage2_2_script/run_paper_cases.py run \
  --sample-index 3 \
  --max-new-tokens 16 \
  --overwrite true \
  --output-root 00_new_codes/repro_autodl/experiments/stage2_2_smoke/experiment1_paper_cases
```

### 停止原因

脚本已进入：

```text
=== Run Layer: Model Load ===
model: Time-HD-Anonymous/STReasoner-8B
attn_backend: flash_attention_2
requested device_map: {'': 0}
```

随后停在 HuggingFace `Fetching 4 files` 阶段较久，没有进入 generate。

本机检查结果：

- `/root/autodl-tmp/cache/huggingface/models--Time-HD-Anonymous--STReasoner-8B` 只有约 `16M`。
- cache 中存在多个 `.incomplete` 文件。
- `/root/autodl-tmp/STReasoner_reproduce/base_model/Config-Qwen3-8B` 只有 config / tokenizer / remote-code 文件，不是完整模型权重。

因此本轮判断为：requirements 环境基本可用，但 STReasoner-8B 权重尚未完整落到当前 cache，smoke test 卡在模型权重获取 / cache 阶段。

### 已生成文件

```text
00_new_codes/repro_autodl/experiments/stage2_2_smoke/experiment1_paper_cases/paper_cases_run.log
```

该日志记录了环境、flash-attn 检查、processor/tokenizer/config 加载和模型加载开始位置。

## 2026-05-28-2

### 本轮结论

已确认上一次卡住的直接原因是 `Time-HD-Anonymous/STReasoner-8B` 权重没有完整下载。本轮完整下载后，1 条样例 smoke test 已通过：

- 数据能读取。
- 模型能加载。
- `generate` 能启动并完成。
- prediction / summary 能保存。
- parser 能在模型输出上被调用。
- attention backend 保持脚本默认 `flash_attention_2`，没有使用 `sdpa`。

本轮仍不是正式完整 paper_cases 实验，只是最小推理链路检查。

### 模型下载与校验

完整模型已下载到：

```text
base_model/STReasoner-8B/
```

4 个权重文件均存在：

```text
model-00001-of-00004.safetensors
model-00002-of-00004.safetensors
model-00003-of-00004.safetensors
model-00004-of-00004.safetensors
```

校验结果：

- `base_model/STReasoner-8B` 总大小约 `16G`。
- 未发现残留 `.incomplete` 文件。
- 4 个 safetensors 文件均可被 `safetensors.safe_open` 打开。
- 已用 symlink 把本地完整模型接入 `/root/autodl-tmp/cache/huggingface`，使脚本硬编码的 repo id 能离线命中本地权重。

### smoke test 结果

执行命令：

```bash
HF_HUB_OFFLINE=1 /root/autodl-tmp/conda/envs/str-py310/bin/python \
  00_new_codes/repro_autodl/experiments/scripts/stage2_2_script/run_paper_cases.py run \
  --sample-index 3 \
  --max-new-tokens 16 \
  --overwrite true \
  --output-root 00_new_codes/repro_autodl/experiments/stage2_2_smoke/experiment1_paper_cases
```

关键结果：

- `model_class`: `Qwen3TSForCausalLM`
- `attn_backend`: `flash_attention_2`
- `model_load_time_sec`: `13.231`
- `generate_success`: `true`
- `parse_success`: `true`
- `actual_new_tokens`: `16`
- GPU 峰值显存约 `15.428 GiB`
- `failure_type`: `no_failure`

输出文件：

```text
00_new_codes/repro_autodl/experiments/stage2_2_smoke/experiment1_paper_cases/paper_cases_run.log
00_new_codes/repro_autodl/experiments/stage2_2_smoke/experiment1_paper_cases/paper_cases_prediction.jsonl
00_new_codes/repro_autodl/experiments/stage2_2_smoke/experiment1_paper_cases/paper_cases_summary.json
```

因此，正式跑 paper_cases 前的最小推理链路检查已经完成。下一步可以在同一环境、同一默认 attention backend 下启动正式实验。
