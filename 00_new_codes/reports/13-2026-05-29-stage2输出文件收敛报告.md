# 13-2026-05-29 stage2 输出文件收敛报告

## 处理目标

Stage 2 / Stage 2.2 当前样本量很小，不需要把 forecasting、non-forecasting、paper cases 分散写成多组 prediction/summary 文件。本次把输出收敛成少数固定文件，后续查结果时优先看统一文件。

## 已调整的脚本

- `00_new_codes/repro_autodl/experiments/scripts/stage2_script/run_smarttest.py`
  - 原来按 case 写 `forecasting_prediction.jsonl`、`non_forecasting_prediction.jsonl`、各自 summary 和 run log。
  - 现在统一写：
    - `predictions.jsonl`
    - `summary.json`
    - `run.log`
  - `--overwrite true` 只替换当前 case/sample 的记录，不会误删另一个 case 已有结果。
  - `prepare` 阶段不再额外写 `prepare_summary.json`，只把准备信息写进 `prepare.log`。

- `00_new_codes/repro_autodl/experiments/scripts/stage2_2_script/run_paper_cases.py`
  - paper cases 数据源改为 `stage2_subsets/paper_cases/PaperCases.jsonl`。
  - stage2_2 准备出的数据文件名改为 `PaperCases.jsonl`，和整理后的数据集命名一致。
  - 运行输出统一写：
    - `predictions.jsonl`
    - `summary.json`
    - `run.log`
  - `prepare` 阶段不再额外写 `prepare_summary.json`。

## 已整理的现有结果

`00_new_codes/repro_autodl/experiments/stage2_results/experiment1_smarttest/` 已从 6 个拆分结果文件收敛为：

- `predictions.jsonl`
- `summary.json`

旧的 `forecasting_*`、`non_forecasting_*` prediction/summary/log 文件已删除。当前目录保留的是两个已有 SmartTest 样本的合并结果。

## 后续查看方式

- 看逐样本原始输出：打开 `predictions.jsonl`，每行是一条运行记录。
- 看实验整体概览：打开 `summary.json`。
- 如果之后重新运行脚本，日志统一写到 `run.log`，不再按任务拆多个 log。

## 注意

本次只是整理输出文件结构和脚本落盘规则，没有重新跑模型推理。
