# 官方与复现分叉对照（Wave 20）

Artifact：[`pipeline_diff_ledger.json`](artifacts/pipeline_diff_ledger.json)。

---

## 1. 三条 pipeline

| 名称 | inference | eval | dtype / tokens | comparable to ST-Test 6144 |
| --- | --- | --- | --- | --- |
| **official** | `inference/inference_tsmllm_vllm.py` | `evaluation/evaluate.py` | half / vLLM / 6144 | baseline |
| **repro_kaggle** | HF generate | stage1 脚本 | ~2048, quant | **false** |
| **repro_autodl_stage2** | stage2 HF/vLLM 混用 | 各脚本 | 6144 部分 | **partial** |

---

## 2. 不可比的具体项（Kaggle）

见 [`12-Kaggle精度链路审计.md`](12-Kaggle精度链路审计.md)：

- 20 题 smoke
- **0** `<answer>` tag
- 无 vLLM worker 路径

---

## 3. stage2 partial 可比

- smarttest / paper_cases / graph ablation：意图对齐 ST-Test，但需 **逐脚本** 核对 `max_tokens`、model path、是否 reuse model。
- graph ablation ledger（阶段二）与官方 run1 **同 evaluate 链** 时，样本级 rescued **161** 可信。

---

## 4. 使用规则

| 用途 | 应用 pipeline |
| --- | --- |
| 主结论 / flip / mismatch | official 6144 三 run |
| 512 预算对照 | official 512 exp |
| quant 资源 | Kaggle **仅** 资源 smoke，不进主表 |
| graph ± | stage2.4 + official evaluate |

---

## 5. Falsifier

统一 pipeline 复跑 Kaggle fp16 20 题后，`with_answer_tag` > 0 且 acc 与 official fp16 同题 Spearman > 0.9；否则保持 **not comparable**。
