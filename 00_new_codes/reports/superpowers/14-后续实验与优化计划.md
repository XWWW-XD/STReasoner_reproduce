# 后续实验与优化计划（Wave 14）

**性质**：**外部部署规格书**——给 AutoDL/A100 或改 `evaluation/`/`inference/` 时用；**不是** superpowers 本工作流的待办队列。

**Superpowers 可执行范围（本仓库会话）**：只读 `exp/` + CPU 脚本 → 阶段一–三 + **E2 + T0-2 + T0-3 + Wave25 已完成**。

每条含 Mechanism → Intervention → Falsifier。

---

## Tier 0 — 零成本（parser / 统计）

| ID | 干预 | 锚点 | Falsifier |
| --- | --- | --- | --- |
| **T0-1** | strict parser：无 `<answer>` 标 parse_fail | `evaluate_qa.py` L39–51 | loose 三不同 ↓ |
| **T0-2** | 报告层 acc_official / acc_parse_ok / parse_fail | superpowers E2+T0-2 | ✅ [`23`](23-T0-2-parse分层与flip归因.md) |
| **T0-3** | `num_tokens` 字段审计（input vs response） | superpowers 只读取证 | ✅ [`25`](25-T0-3-num_tokens字段取证.md) |

---

## Tier 1 — 单旋钮推理（需 GPU，仍 ST-Test 官方 pipeline）

| ID | 干预 | 锚点 | Falsifier |
| --- | --- | --- | --- |
| **T1-1** | temperature 0 → 0.2 对照 **仅** flip 集 | `SamplingParams` worker L162 | flip 率应 ↓；若不变则 instability 非采样主因 |
| **T1-2** | max_tokens 6144→4096 + 强制 `` stop | L162 `max_tokens=CTX_LENGTH` | 无 tag 率 ↓、unclosed numeric loop 消失 |
| **T1-3** | 生成后 truncate：遇重复 float 模式截断 | 新 postprocessor（inference 侧） | idx 78/191 类样本 response 长度 <3k 且 parse_ok |
| **T1-4** | Graph ablation **固定** checkpoint 重跑 4 paper cases | stage2.4 regex 去 graph | rescued/harmed 符号与 [`10`](10-graph消融样本级.md) 同向 |

---

## Tier 2 — 评测口径扩展

| ID | 干预 | Falsifier |
| --- | --- | --- |
| **T2-1** | Entity/eti **loose strict**：Node+数字窗口 vs raw patch | loose 叙述 591/1194 中应出现 >0 strict mismatch |
| **T2-2** | Forecasting：thinking 内数值 replace 后 offline MAE | persistent core local_drift 子集 MAE 应改善 |
| **T2-3** | 「复述 fidelity loss」仅 logging，不改训练 | 高 mismatch 样本 log prob 不高于低 mismatch |

---

## Tier 3 — 训练（高风险，最后）

| ID | 干预 | 锚点 | Falsifier |
| --- | --- | --- | --- |
| **T3-1** | SFT 仅对 `<answer>` 段计 loss | `supervised.py` L82–96 | thinking 胡写 ↓ 但 acc 不降（需全量 eval） |
| **T3-2** | 辅助 loss：patch 复述 MSE | `TimeSeriesEmbedding` + labels | strict mismatch 率 ↓（correlation 子集） |

---

## 实验包 E1–E4（外部；superpowers 不执行）

| 包 | 内容 | 依赖 | superpowers |
| --- | --- | --- | --- |
| **E2** | T0-1 全量 reparse 三 run 6144 | CPU | **✅** [`22`](22-E2-strict-reparse全量对照.md) |
| **T0-2** | parse 分层 + flip 归因 | CPU | **✅** [`23`](23-T0-2-parse分层与flip归因.md) |
| **T0-3** | num_tokens 字段审计 | CPU | **✅** [`25`](25-T0-3-num_tokens字段取证.md) |
| **Wave25** | 真实不稳定 flip 账本 | CPU | **✅** [`24`](24-真实不稳定flip账本.md) |
| **E4 idx** | 分层 idx 列表（供 GPU 重跑） | CPU | **✅** `stratified_idx_lists_e4.json` |
| **E1** | T1-2 + T1-3 smoke 50 题 | A100 + 官方 vLLM | 仅规格 |
| **E3** | Paper cases 4 题 × graph ± | stage2.2 + GPU | 仅规格 |
| **E4** | Graph 全量 w/o 重跑 | GPU + 对齐配置 | 仅规格 |

**E2 结果**：`e2_strict_reparse_summary.json`；correlation 三不同 **67→18** falsifier 通过。

---

## 代码级 Intervention 锚点（阶段三增补）

来自 [`code_experiment_crosswalk.json`](artifacts/code_experiment_crosswalk.json)：

| 现象 | 文件:概念 |
| --- | --- |
| no_answer / unclosed | `inference` max_tokens + `evaluate` `_extract_tag_content` fallback |
| loose 三不同 | `evaluate_qa.load_prediction_files` + `_normalize_choice` |
| flip ⊥ mismatch | evaluate 不看 thinking；SFT 全文 CE |
| graph +6pp | prompt graph 段；stage2.4 regex remove |
| Kaggle 不可比 | `repro_kaggle` HF pipeline vs `inference_tsmllm_vllm.py` |

深读行号见 [`16`](16-inference通路深读.md)–[`21`](21-代码与实验异常对照.md)。

---

## 明确不做

- 用 Kaggle 20 题 quant 结果优化官方 ST-Test 结论
- 在 loose parser 下解读「三 run 三不同」
- 无 stop/truncate 情况下继续加 max_tokens
