# 现有数据第三波挖掘

回应：Superpowers 是 **迭代归纳**，不必在首轮统计后停；在 **仍只用 run1/2/3 的 exp** 前提下，本报告继续挖 [`03`](03-深化探索与终止说明.md) 未覆盖的维度。脚本：[`_analysis/compute_wave3.py`](_analysis/compute_wave3.py)。

---

## 1. 新发现摘要

| 主题 | 结论 |
| --- | --- |
| **常数填充仍最「好答」** | correlation persistent 中 `constant_fill` 三 run 平均 acc **87.9%**，`local_drift` 仅 **78.3%** |
| **Flip 题 thinking 更长** | correlation flip 组 think_len median **2534** vs 稳定对题 **2061**（+470 字符） |
| **Entity loose 叙述** | 宽口径 Node+数字 vs 几乎无数值：**acc 无差**（~73.5% vs 74%） |
| **三 run 三选项全不同** | correlation **67**、entity **40**（**loose** 口径）→ wave4 修正：**strict 单字母三不同仅 18+31** |
| **Forecasting MAE 谁最好** | 275 题中 run2/run3 各优于 run1 **135–138** 题，解释全局 MAE↓ |
| **图规模 × mismatch** | correlation 大网（≥7 nodes）mismatch 率 **22%** > 小网 **20%**；forecasting 小网反而更高 |
| **序列长度** | forecasting 短序列（≤48）mismatch 率 **58%** > 长序列 **40%** |
| **Persistent 严重度 drift** | forecasting persistent 的 max_diff run 间 range median **3.2**；correlation **8.0**（复述「错多少」仍随 run 变） |

---

## 2. 错误类型 × 答对率（correlation persistent）

[`class_vs_correctness.json`](artifacts/class_vs_correctness.json)（190 题，三 run 平均）：

| 分型 | n | mean acc（三 run） |
| --- | ---: | ---: |
| constant_fill | 66 | **0.879** |
| other_large_error | 46 | 0.891 |
| magnitude_offset | 11 | **1.000** |
| local_drift | 66 | **0.783** |
| spike_hallucination | 1 | 1.000 |

**补分析**：在 **persistent 复述错** 子集内，**local_drift（数值形状略偏）反而更常答错**；**常数填充/大偏差** 与「写错仍选对」一致，说明模型常 **用错误数字做 narrative 再蒙对选项**，而非「读数越准越对」。

---

## 3. Thinking 密度与 flip

[`thinking_density_profiles.json`](artifacts/thinking_density_profiles.json) + [`flip_thinking_contrast.json`](artifacts/flip_thinking_contrast.json)：

**Correlation**

- mismatch_run1：broad mention median **4.0** vs 全体 **3.0**（[`count_ts_mentions`](../../tools/mlp_encoder_focused_analysis/count_ts_mentions.py) 宽口径）。
- **Flip 组 think_len 更长**（2534 vs 2061），broad mention **略少**（3 vs 3.33）→ flip 更像 **推理链变长/变轨**，不是「多写几个数就稳」。

**Entity flip**

- broad mention **无差**（1.33 vs 1.33）；flip 仍 357 题 → entity 波动 **与口头列数无关**。

**Etiological flip**

- flip 组 broad **3.33** vs stable **2.0**（+1.33）→ 小任务里「写更多 TS 叙述」与 flip 略相关。

---

## 4. Entity / Etiological：loose 叙述 ≠ strict mismatch

[`loose_vs_accuracy.json`](artifacts/loose_vs_accuracy.json)（run1 分组，三 run acc）：

| task | 组 | n | mean acc |
| --- | --- | ---: | ---: |
| entity | loose_only | 594 | 0.735 |
| entity | no_numeric | 600 | 0.740 |
| etiological | loose_only | 142 | 0.918 |
| etiological | no_numeric | 65 | **0.980** |

**解读**：etiological 上 **少写数字反而更对**（可能与题型更简单有关，非因果）；entity 两档 **几乎相同**——宽口径「读数」不是 acc 主因。

---

## 5. 三 run 选项多样性

[`answer_diversity.json`](artifacts/answer_diversity.json)（**loose**，与 evaluate extracted pred 一致）：

| task | 三 run 同答案 | 两档不同 | **extracted 串三不同** |
| --- | ---: | ---: | ---: |
| correlation | 1289 | 236 | **67** |
| entity | 789 | 365 | **40** |

**wave4 修正**：其中 **strict 单字母 A–D 三不同** 仅 correlation **18**、entity **31**；详见 [`05-第四波案例册与口径修正.md`](05-第四波案例册与口径修正.md)。

---

## 6. Forecasting：MAE 与 run 选择

[`forecasting_mae_ranking.json`](artifacts/forecasting_mae_ranking.json)：

- **best run 计数**：run1 **91** / run2 **91** / run3 **93**（275 题）——无单 run 垄断。
- run2、run3 **各约 135–138 题** MAE 优于 run1 → 与全局 MAE 64 vs 68 **一致**。
- MAE range top：idx **147** range=548（非 persistent）；idx **19** persistent range=249。
- **Persistent 不防 MAE 大波动**：19/76/173 等在 persistent 集且 range>50。

---

## 7. 输入复杂度 vs mismatch（run1）

[`graph_complexity_vs_mismatch.json`](artifacts/graph_complexity_vs_mismatch.json) + [`series_length_vs_mismatch.json`](artifacts/series_length_vs_mismatch.json)：

**Forecasting**：短序列 mismatch **58%**；长序列 **40%**。小图（≤3 nodes）**53%**。

**Correlation**：序列长度与 mismatch 率 **差异小**（16–23%）；大网（≥7 nodes）**22.5%** vs 小网 **20%**。

→ forecasting 的复述问题 **更集中在短序列/小图**；correlation 更 **均匀**。

---

## 8. Persistent 严重度：run 间仍 drift

[`persistent_severity_stability.json`](artifacts/persistent_severity_stability.json)：

| task | max_diff range median | range>50 题数 | range>200 |
| --- | ---: | ---: | ---: |
| forecasting (104) | **3.23** | 10 | 3 |
| correlation (190) | **7.96** | 36 | 7 |

Forecasting persistent：**错窗集合稳定、错幅 stable**；correlation persistent：**集合半稳定（§03）但错幅 run 间仍晃**（如 idx 872 range 448）。

---

## 9. 对「终止」的修正

[`03`](03-深化探索与终止说明.md) §6 的「现有 JSON 挖尽」 **过早**。本波证明仍可挖：

- 分型 × acc、flip × think 特征、MAE run 账本、图/序列分层、选项多样性、persistent 严重度 drift。

**仍需要新推理才能做**（边界未变）：受控复述 probe、thinking 修正 evaluate。

**仍可在现有数据继续**（建议 wave 4，若继续）：

1. 67+40 题「三选项全不同」案例册 + 与 flip 矩阵交叉。
2. correlation `local_drift` vs `constant_fill` 的 **raw response 片段对比**（规则抽 excerpt）。
3. forecasting idx 147/76 等 **高 MAE range 非 persistent** 与 persistent 19 的对照深描。
4. 三 run **逐题 MAE/acc 方差排名** 导出 CSV 供人工扫。
5. 宽口径 mention 与 strict mismatch 的 **样本级 Jaccard**（run1）。

---

## 10. 附件

| 文件 | 内容 |
| --- | --- |
| [`loose_vs_accuracy.json`](artifacts/loose_vs_accuracy.json) | entity/etiological 分组 acc |
| [`thinking_density_profiles.json`](artifacts/thinking_density_profiles.json) | 分组 mention/长度 |
| [`class_vs_correctness.json`](artifacts/class_vs_correctness.json) | persistent 分型 × acc |
| [`answer_diversity.json`](artifacts/answer_diversity.json) | 三 run 选项多样性 |
| [`forecasting_mae_ranking.json`](artifacts/forecasting_mae_ranking.json) | MAE 与 best run |
| [`flip_thinking_contrast.json`](artifacts/flip_thinking_contrast.json) | flip vs stable mention |
| [`graph_complexity_vs_mismatch.json`](artifacts/graph_complexity_vs_mismatch.json) | 图规模分层 |
| [`series_length_vs_mismatch.json`](artifacts/series_length_vs_mismatch.json) | 序列长度分层 |
| [`persistent_severity_stability.json`](artifacts/persistent_severity_stability.json) | persistent max_diff drift |
| [`_analysis/compute_wave3.py`](_analysis/compute_wave3.py) | 本波脚本 |

---

## 11. 补分析

- **Superpowers 合适停下来的信号** 应是「新波次 **重复验证旧结论**、无新 RQ」，而非「artifact 数量够了」。
- 本波 **新 RQ（事后）**：RQ5 输入复杂度是否调制 mismatch？→ forecasting **是**（短序列↑），correlation **弱**。
- 本波 **新 RQ6**：persistent 分型是否调制 acc？→ **是**，local_drift 显著更差。
- 本波 **新 RQ7**：flip 是否由 thinking 更长驱动？→ correlation **是**（+470 字符），entity **否**。
