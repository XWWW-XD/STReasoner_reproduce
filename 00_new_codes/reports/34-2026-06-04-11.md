# 2026-06-04-11 Stage 1 preflight/postcheck 辅助脚本日志

## 本轮范围

继续做不训练的小任务：新增训前 preflight 和训后 postcheck 脚本，验证它们能作为下次继续训练的固定入口。

## 新增脚本

```text
00_new_codes/repro_autodl/experiments/scripts/single_a100_qwen3_4b_stage1_lora_preflight_2000to2500.sh
00_new_codes/repro_autodl/experiments/scripts/single_a100_qwen3_4b_stage1_lora_2500_postcheck.sh
```

## preflight 做什么

- 检查模型目录、STReasoner TS config/code、Stage 1 数据。
- 检查 2000 save_state 的 `checkpoint-500` 是否有完整 resume state。
- 如果 2500 输出目录已存在则停止，避免混合旧输出。
- 对续训脚本做 `bash -n`。
- 打印磁盘和 GPU 状态。

本轮已运行通过：

```text
ready_to_train=ok
/ = 11%
/root/autodl-tmp = 66%
GPU = idle, 1MiB / 40960MiB
```

## postcheck 做什么

2500 训练完成后运行：

- 检查 `train_results.json`、`trainer_state.json`、最新 checkpoint state。
- 跑标准 adapter load check。
- 跑 30 条健康探针。
- 跑 40 条 temporal balanced probe。
- 写日志和 jsonl 结果。

当前 2500 输出目录尚不存在，因此本轮只做语法检查，没有运行 postcheck。

## 下次顺序

```bash
00_new_codes/repro_autodl/experiments/scripts/single_a100_qwen3_4b_stage1_lora_preflight_2000to2500.sh
00_new_codes/repro_autodl/experiments/scripts/single_a100_qwen3_4b_stage1_lora_resume_2000to2500_save_state.sh
00_new_codes/repro_autodl/experiments/scripts/single_a100_qwen3_4b_stage1_lora_2500_postcheck.sh
```

第二条会启动训练，必须等用户明确允许。
