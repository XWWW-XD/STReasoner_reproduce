# 13-2026-05-29-2

- 断点：用户要求暂停，原因是额度不够。
- 当前不要继续运行模型、不要继续改代码。
- 已完成：
  - `run_paper_cases.py`：`run-all` 改为一次加载模型复用 4 条样例；修复 `failure_type_from()` 参数数量错误。
  - `evaluation/evaluate_qa.py`：增强选择题和 forecasting parser。
  - 已生成 `parserfix_reparse_existing_6144`：4/4 generate，4/4 parse，选择题 3/3，forecasting MAE 0.1667。
  - 开始处理“模型不按 `<answer>` 格式输出”的问题：`run_paper_cases.py` 已新增格式诊断/`formatted_answer` 相关函数和字段。
  - 已基于已有输出生成 `formatfix_reparse_existing_6144`，raw format success 为 0/4，但可生成规范化 `formatted_answer`。
- 尚未完成：
  - 还没复跑新脚本确认新字段会在正式 `run-all` 输出中保存。
  - 还没把“格式层修复”和 “2.3 ST-TEST 数据集验证论文真实效果” 追加进 `11_2.2paper_cases重试修复报告.md`。
  - 还没更新 `agents/readme.md` 关于 raw format / formatted_answer 的经验。
- 下次恢复建议：
  - 先 `py_compile`。
  - 再看 `git diff`，确认只包含预期修改。
  - 不急着复跑模型；可先完善报告，把 ST-TEST 现状写清楚：目前只有 SmartTest 2 条，不足以验证论文真实效果，真正验证需要完整 ST-Test 四类任务。
