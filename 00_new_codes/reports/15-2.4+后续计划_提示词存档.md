
## 1 **附录定性对照**

4 条 paper case 的 raw CoT 与论文 Appendix 逐条比（定性，不算分）。

## 2 real world dataset

是如何基于 **CausalRivers** 构造的 causal QA？

## 3 TS encoder结构

## 【已做】w/o spatial reasoning 效果对比

```提示词
Goal（阶段 2.4）：同一 STReasoner-8B，对比 w/ graph（原 input）与 w/o graph（用 remove_graph_structure() 删 Graph Structure:...，timeseries 不变），测 tag-first evaluate，不改 raw/gold。

样本：先做paper_cases的4个样例，得到对比，并写报告。然后报告给我效果，再做下一步ST-TEST。该样本集全体样本均需要测试，根据样本task分为4次分别实验分别记录，最后汇总写报告。

预期实验结果：对应论文的figure 6。你需要先去论文找到对效果的描述，将预期效果写入报告，如果找不到可以问我。

操作：对每条样本两条 prompt，分别是
- w/ graph：原始 input
- w/o graph：用与 EasyR1 同一正则删 Graph Structure:... 到 please analyze 之前（timeseries 不变）
指标（都用 tag-first evaluate，不改 raw）：

推理：官方 inference_tsmllm_vllm.py，max_tokens=6144，加 prompt.json；evaluate 显式 --dataset data/ST-Bench/...，PYTHONPATH=.

指标：尽可能详细，记录你认为需要的所有指标。

硬规则：见 agents修改文件必读规则.md；如何写报告可参考reports文件夹中的类似报告。
```

## 4 多checkpoint对比实验


```
Goal:
完成 STReasoner checkpoint 对比实验。

checkpoint地址：在huggingface上找ST-Reasoner相关模型

样本与流程：
先做paper_cases的4个样例，得到对比，并写报告。然后报告给我效果，再做下一步ST-TEST。该样本集全体样本均需要测试，根据样本task分为4次分别实验分别记录，最后汇总写报告。

Context:
当前项目已经能跑通并复现出效果。现在需要比较不同 ST-Reasoner checkpoint 的实验表现，观察模型效果是否随 checkpoint 变化而变化，并如实记录真实实验结果。

预期实验结果：可能是效果逐渐变好？也可能是别的，请你以真实的实验效果编写报告

Constraints:
不要修改 raw response。
不要做答案格式修复。
不要用 gold_answer 辅助解析、筛选或重跑。
评测使用现有 tag-first evaluate 逻辑，尽量复用已有代码，不要重写官方 evaluate。
如果 checkpoint、路径、脚本参数或数据格式不清楚，先停止并汇报，不要自行改变实验目标。

指标：尽可能详细，记录你认为需要的所有指标。

硬规则：见 agents修改文件必读规则.md；如何写报告可参考reports文件夹中的类似报告。
```

## 5 数据集调研

有哪些可用的类似的数据集？
它们的数据格式和本代码支持的数据格式的差别？
对可行性和效果进行分析预测

确定ST-Casual是否对应论文 5.2 的 real-world zero-shot causal QA dataset？还是对应什么其他内容？
该数据集是否有其他论文使用过？效果如何？

## 6 encoder源码阅读

Encoder 读懂 + 一张 shape 表（与 2 并行）
一条样本：文字 token 数、patch 数、merge 后长度。
→ 回应老师说的 窗口；组会上能讲清楚结构。

# 先不做

## 参数敏感性测试

## 可能探索的问题

「STReasoner 在推理时对 graph 文本的依赖，四类 task 是否不同？」（你正在做的）
「patch_size 改变时，forecasting MAE 与 reasoning accuracy trade-off 如何？」（要重训，适合作为 下一阶段 课题）