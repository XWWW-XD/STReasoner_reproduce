# 2026-06-12 日志 1 — 新实例环境配置与代码通读

日期：2026-06-12

## 结论（先写）

1. **环境**：`/root/autodl-tmp/conda/envs/str-py310` 已装好 SFT 依赖（torch 2.6+cu124、vllm 0.8.5、deepspeed、flash-attn）；`bitsandbytes==0.45.2`（非 requirements 原文 0.43.1）。
2. **下载加速**：pip 清华源 + 数据盘缓存；flash-attn 用 ghproxy 预下载 wheel；HF 用 `HF_ENDPOINT=https://hf-mirror.com`；conda 已有清华镜像。
3. **未完成**：训练集（ST-Align/CoT/RL）和模型权重（4B/8B）本实例尚未下载；ST-Test 四类 jsonl 已在 git。
4. **此前失误**：未先读 `guides/修改文件必读规则.md`，报告写在 `reports/t2-autodl复现阶段/` 且未按 `XX-YY-MM-DD-id` + `HH:MM` 日志格式；本文件为纠正后的正式日志。

重装脚本：`00_new_codes/repro_autodl/setup_str_env.sh`

---

## 对话阶段日志（按规则原样记录）

10:03：用户要求拉取仓库 `https://github.com/XWWW-XD/STReasoner_reproduce`。

10:05：尝试 `git clone`；本机 `127.0.0.1:7897` 代理不可用导致失败。

10:06：取消代理后克隆成功，路径固定为 `/root/autodl-tmp/STReasoner_reproduce`。

10:08：用户问能否设为 `autodl-A800` 分支；远程仅有 `main`/`autodl`/`local`，无 `autodl-A800`。

10:08：从 `origin/main` 创建本地分支 `autodl-A800`；说明需 `git push -u origin autodl-A800` 后才能直接 `git pull`。

10:12：用户要求通读代码库并在 reports 写 git 说明。

10:12：读了 README、`cache_config.py`、`.gitignore`、现有 reports 结构；在 **`t2-autodl复现阶段/14-Git操作说明.md`** 写了 git 文档（**未遵守 guides 命名与位置**）。

10:15：用户要求用 superpowers 通读代码库、配置环境、解决 AutoDL 下载慢、写 reports。

10:15：读了 `requirements.txt`、`cache_config.py`、部分 reports；创建 conda env、配置镜像、安装依赖；在 **`t2-autodl复现阶段/15-AutoDL-A800环境配置与下载加速.md`** 写了环境报告（**同样未按 guides 规则**）。

10:20：用户反馈「根本没看你写报告和日志」，质疑是否读过代码。

10:22：用户追问「读完代码不该看到 guides 等规则吗？」——确认应先读 `guides/修改文件必读规则.md`、`pipeline_map.md`、`EXPERIMENTS.md` 等。

10:25：补读本日志；按 `46-2026-06-12-1` 格式写入 `00_new_codes/reports/`。

---

## 通读代码库（结合 guides，非仅 README）

### 应优先读的 guides（本次补读）

| 文件 | 要点 |
| --- | --- |
| `guides/修改文件必读规则.md` | 报告格式、python 路径、ST-Test 6144、禁止改 response、存档目录、单卡训练经验 |
| `guides/pipeline_map.md` | 官方三阶段 vs 复现 Stage 编号区分；数据/训练/推理/评测入口 |
| `guides/EXPERIMENTS.md` | `repro-canonical` = `exp/sttest_full_*_6144/`；upstream = `exp_STReasoner-8B/` |
| `guides/分析入口说明.md` | 分析脚本放 `scripts/<任务>/`，报告放 `reports/` |

### 官方数据通路（根目录代码）

- **缓存**：`cache_config.py` 在 AutoDL 下强制 `HF_HOME=/root/autodl-tmp/cache/huggingface`；`train.py`、`inference_tsmllm_vllm.py` 入口均 `apply_cache_config()`。
- **数据注册**：`data/dataset_info.json` 映射 `ST-Bench/ST-Align`、`ST-CoT`、`ST-RL` 等到 LLaMA-Factory 列 `input/output/timeseries`。
- **训练**：`src/train.py` → `llamafactory.train.tuner.run_exp`；`scripts/qwen3-8b/train_stage1.sh` 用 DeepSpeed ZeRO-3、`template STReasoner-Align`、`timeseries_sft_lr`。
- **TS encoder**：`base_model/Config-Qwen3-8B/modeling_qwen3_ts.py` 中 `TimeSeriesEmbedding` + `ts_encoder`；`initial_model.py` 对 `ts_encoder` 做 Xavier 初始化后写回权重目录。
- **推理**：`inference/inference_tsmllm_vllm.py` 注册 `inference.vllm.chatts_vllm`，`LLMClient(engine="vllm-ts")`，`TS_PATCH_SIZE=8`；默认 `SamplingParams max_tokens=512`（正式 ST-Test 须 CLI 传 6144）。
- **vLLM worker**：`inference/llm_utils.py` 的 `worker_vllm_ts` 在 L186–188 已支持调用方传入 `SamplingParams`（guides 强调须确认此点）。
- **评测**：`evaluation/evaluate.py` → `evaluate_qa.evaluate_predictions_for_task`；只读预测 `response`，不改写。

### 与 guides 对齐的当前实例状态

| 检查项 | 状态 |
| --- | --- |
| Python | `/root/autodl-tmp/conda/envs/str-py310/bin/python` 3.10.20 ✓ |
| ST-Test 四类 | `data/ST-Bench/ST-Test/*.jsonl` ok ✓ |
| ST-Align/CoT/RL | missing（需 `download_dataset.py`） |
| STReasoner-8B | 本实例 `base_model/` 仅 Config，无权重 |
| repro-canonical 输出 | `exp/sttest_full_*_6144/` 待后续实验产生 |

---

## 环境配置与下载加速（证据）

| 项 | 配置 |
| --- | --- |
| conda env | `/root/autodl-tmp/conda/envs/str-py310`（~8.9G，数据盘） |
| pip | `/root/.pip/pip.conf` → 清华源，`cache-dir=/root/autodl-tmp/cache/pip` |
| HF | `HF_ENDPOINT=https://hf-mirror.com`，缓存 `/root/autodl-tmp/cache/huggingface` |
| flash-attn | ghproxy 下载 wheel → `cache/wheels/` 本地 pip |
| torch | `pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124` |
| bitsandbytes | `0.45.2`（兼容 triton 3.2；guides 单卡训练经验已记录同问题） |
| bashrc | HF 缓存、`TORCH_EXTENSIONS_DIR`、`PATH` 已写入 |

验证：`torch 2.6.0+cu124 cuda True`，`vllm 0.8.5`，`deepspeed 0.16.4`，`import bitsandbytes` ok。

---

## 下一步（按 guides）

```bash
cd /root/autodl-tmp/STReasoner_reproduce
export HF_ENDPOINT=https://hf-mirror.com
/root/autodl-tmp/conda/envs/str-py310/bin/python download_dataset.py
# 模型按需 download_model.py + cp Config + initial_model.py
bash 00_new_codes/repro_autodl/experiments/scripts/single_a100_qwen3_4b_env_check.sh
```

正式 ST-Test 推理须 `max_tokens=6144`，输出进 `exp/sttest_full_*_6144/`，至少 push `generated_answer.json` + `evaluation_metrics.json`。

---

## 误放文件说明

| 路径 | 说明 |
| --- | --- |
| `reports/t2-autodl复现阶段/14-Git操作说明.md` | git 说明，内容可参考，但非标准日志编号 |
| `reports/t2-autodl复现阶段/15-AutoDL-A800环境配置与下载加速.md` | 环境说明草稿，正式记录以本文件为准 |
