# 2026-06-04 日志 4

18:51：用户要求降低巡视频率。先确认当前没有训练、deepspeed、torchrun、vLLM 残留进程，GPU 空闲，系统盘 `/` 使用率约 `11%`，数据盘 `/root/autodl-tmp` 使用率约 `64%`。

18:52：复查 `22-Stage1单卡4B实验.md`、已有 `500` step 训练产物和生成检查脚本。当前有效断点仍是：`500` step LoRA adapter 可标准加载、可进入生成链路，但第 0 条训练样例输出 `1.00`，gold 为 `8.0`。

18:52：统计 `ST-Align/alignment_train.jsonl` 类别分布：

```text
total=153700
spatial=70944
temporal=63943
spatial_temporal=18813
```

18:52：新增 `single_a100_qwen3_4b_stage1_adapter_probe.py`，用于一次加载 `500` step adapter 后连续跑少量训练样例。这个脚本不是正式 evaluate，只用于判断 adapter 输出形态，避免只看第 0 条样例。

18:52：运行 6 条样例探针：

```text
sample_indices=0,1,71,72,121,122
max_new_tokens=32
adapter=output/single_a100_qwen3_4b_stage1_lora_500steps
```

18:53：探针完成。结果：

```text
0 temporal gold=8.0 response=1.00 match=false
1 temporal gold=0.0654 response=1.0 match=false
71 spatial gold=no response=no match=true
72 spatial gold=no response=no match=true
121 spatial_temporal gold=demand_source response=0.00 match=false
122 spatial_temporal gold=demand_source response=0.00 match=false
probe_exact_match_simple=2/6
cuda_max_memory_allocated_gb=8.50
cuda_max_memory_reserved_gb=8.60
```

18:54：更新 `20-Stage1正式训练计划.md` 的最新断点，明确写入 `500` step 多样例探针结果：链路可用、输出不再乱码，但 `500` steps 不足以证明 Stage 1 学会。

18:55：更新 `22-Stage1单卡4B实验.md`，追加 `500 step 多样例生成探针` 章节，记录样例选择、日志路径、结果表和解释。

当前判断：

- 今天已经完成 Stage 1 单卡 4B LoRA 的有效 smoke、100 step、500 step、小样例探针。
- 不建议今晚继续长训练；如果明天继续，应优先写 resume 脚本，从 `500` step adapter 继续到 `1000` steps 或更高，再复用同一组探针比较。
- 不进入 Stage 2/3。
