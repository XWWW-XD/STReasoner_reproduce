# 目标：跑通实验

### 提示词1

正式跑 paper_cases 前，请先阅读相关代码并做一次推理链路 smoke test，不要求得到完整实验结果。

需要先阅读：
- Stage 2.2 脚本、05_stage2_2_paper_cases实验脚本修改报告
- 它调用的数据读取逻辑
- 它调用的模型加载 / generate / inference 逻辑
- 它调用或复用的 evaluate/parser 逻辑
- Stage 1 prompt2文件 / README / 输出规范

然后做一次最小推理测试：
- 只跑 1 条样例或最小 batch
- 目标是确认：数据能读、模型能加载、generate 能启动、输出能保存、parser 能被调用
- 不要求该样例预测正确
- 不要求跑完整 paper_cases
- 如果卡在模型加载、显存、路径、依赖、cache、parser import 等问题，请停止并汇报，不要盲目改大段代码

### 提示词2

现在 smoke test 已通过。请使用现有 Stage 2.2 脚本，正式在 paper_cases 数据集上跑 2 次实验，实验结果都做记录。

实验目的：
本次实验是 Stage 2.2 的 paper_cases 论文样例补实验。目标是验证当前 Stage 2.2 推理脚本在论文样例数据上的实际表现：能否读取 paper_cases、完成生成、复用/接近官方 evaluate parser 完成解析，并观察是否存在格式失败、重复生成、达到 max_new_tokens、缺少 answer tag 等问题。结果用于补充组会汇报中的“论文样例复现实验”。

运行要求：
1. 不要重写代码，不要重新设计实验，不要改 prompt。
2. 使用 Stage 2.2 脚本。
3. 数据路径必须是：00_new_codes/repro_kaggle/experiments/stage1_subsets/exp1_resource_tiny20/paper_cases
4. max_new_tokens 必须是 6144。

运行完成后请参考report中的prompt2 和 00_new_codes文件夹中的其他实验记录和报告，写报告，尽量写清楚你认为需要的所有指标，不要遗漏。

### 提示词3

写清楚原始结果的报告后，请开始排查代码，尝试查找为什么这些样例的结果是错误的。并修改代码，努力让结果正确。

要求：  
1. 先诊断，不要直接改代码。每次怀疑都要给出涉及文件、函数、行号和证据。  
2. 只有确认是代码 bug 时，才做最小修改。  
3. 禁止为了让样例变正确而硬编码 gold answer、修改 gold answer、删除失败样例、放宽指标、或者改 prompt 诱导某个样例答案。  
4. 每次修改后都要重新运行paper_case，并记录修改前后结果差异。

请在 reports 文件夹中新建一个独立 md 文件，完整记录：原始结果、排查过程、证据、修改位置、修改原因、修改后结果、仍未解决的问题。

做完后对00_new_codes中新增的文件尽量整理一下，在readme中添加一下索引和简要1-2句话说明。

