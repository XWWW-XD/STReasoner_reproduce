# 2026-06-04 日志 3

17:58：继续 Stage 1 目标，先不盲目扩训，按 `22` 报告中的建议补做最小生成链路检查。

18:00：阅读 `processing_qwen3_ts.py` 和 ST-Align 第 0 条样例，确认数据里 `input` 保留 `<ts><ts/>` 占位，`timeseries` 是原始数组，processor 会负责 offset/scaling 文本和 `timeseries` tensor。

18:01：新增 `single_a100_qwen3_4b_stage1_adapter_generate_check.py`，用于加载 base model + adapter，并对 ST-Align 第 0 条做 `max_new_tokens=16` 的最小生成检查。

18:02：用 `100` step adapter 跑生成检查，链路通过，输入形状为 `input_ids_shape=(1, 492)`，`timeseries_shape=(5, 192, 1)`，但输出是乱码，不评价准确率。

18:03：决定扩到 `500` steps，新增 `single_a100_qwen3_4b_stage1_lora_500steps.sh`。配置仍为 bf16、FlashAttention-2、显式 Qwen LoRA target、自动追加 `ts_encoder.mlp.*`，不使用 `additional_target=ts_encoder`。

18:04：启动 `500` step Stage 1 LoRA 训练，输出到 `output/single_a100_qwen3_4b_stage1_lora_500steps/`。

18:04：训练日志确认使用 FlashAttention-2，trainable params 为 `16,700,480`，总步数为 `500`，`Applied SFT learning rate 0.000010 to 10 time-series encoder parameter(s).`

18:12：训练约到 `100/500`，速度约 `5` 秒/step，无 NaN 或中断。

18:20：训练约到 `223/500`，用户要求降低巡视频率，后续改为更低频巡查。

18:30：训练约到 `352/500`，仍稳定。

18:40：训练约到 `478/500`，接近结束。

18:45：`500` step 训练完成，`train_runtime=2475.1534` 秒，约 41 分 15 秒，`train_loss=3.8557742080688477`。

18:46：对 `500` step adapter 做标准加载检查，`PeftModelForCausalLM` 加载成功，`nonzero_lora_B=257/257`，结果为 `adapter_load_check=ok`。

18:46：对 `500` step adapter 做最小生成检查，ST-Align 第 0 条 gold 为 `8.0`，模型输出 `1.00`，链路通过但答案不正确，不能写成效果达标。

18:48：更新 `22-Stage1单卡4B实验.md`，追加 `500` step 指标、加载检查、生成检查和后续建议。
