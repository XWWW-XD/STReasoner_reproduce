# 单卡 A100 小模型训练复现可行性计划

## 结论

目标：用比 8B 小的模型试着从头训练

资源：单张 A100-PCIE-40GB

计划：做“小模型、缩步数、缩 batch、链路完整”的训练流程复现。
- 第一推荐模型是 `Qwen/Qwen3-4B-Instruct-2507`，不是 1.7B/0.6B。
- 1.7B/0.6B 虽然更省显存，但当前仓库没有对应的 STReasoner time-series config 和训练脚本；从它们开始会先变成模型结构适配任务，风险比 4B 高。

当前任务状态：
- 已准备 4B 模型和 ST-Bench 训练数据。
- 已做 Stage 0 环境门禁。
- 已尝试 Stage 1 10-step smoke，但没有进入真实训练 step。
- `bitsandbytes/triton.ops` 和 `ninja` 两个依赖断点已处理。
- 当前真实断点变为 4B full fine-tuning + ZeRO-3 CPU offload 初始化 optimizer states 时超过容器内存上限。
- 现在不要继续重复 full fine-tuning smoke，下一步应改为 LoRA / QLoRA / 只训 TS encoder，或换更小模型。

## 系统盘事故复盘

所以系统盘接近 100% 的直接原因不是当前 HF cache，而是 checkpoint 实验过程中留下的 `/root/model_archive` 模型副本。后续规则：

- 模型、HF cache、Torch cache、Triton cache 都必须落到 `/root/autodl-tmp`。
- 不再把模型归档放进 `/root/model_archive` 或 `/root/.cache`。
- 下载/加载大模型前先查 `df -h / /root/autodl-tmp` 和 `du -sh /root/model_archive /root/.cache /root/autodl-tmp/cache`。
- 实验结束必须检查是否有系统盘模型副本残留。

## 当前真实断点

当前已完成：

- stage2.5 相关临时结果和脚本已清理。
- 系统盘 `/root/model_archive` 中的 STReasoner checkpoint 副本已清理，系统盘占用从 `93%` 降到 `7%`。
- 单卡 4B smoke 脚本已写好。
- 已下载并初始化 `Qwen/Qwen3-4B-Instruct-2507` 到数据盘：
  - 目录：`base_model/Qwen3-4B-Instruct-2507/`
  - 下载完成后已复制 `base_model/Config-Qwen3-4B-Instruct-2507/` 的 STReasoner TS config。
  - 已运行 `initial_model.py --model_path base_model/Qwen3-4B-Instruct-2507`，模型能加载，`ts_encoder` 已初始化。
  - 最终模型为 2 个 fp16 safetensors 分片：`model-00001-of-00002.safetensors`、`model-00002-of-00002.safetensors`。
  - 最终目录约 `8.3G`，抽查 dtype 为 `torch.float16`。
  - 系统盘 `/` 保持 `7%`，`/root/.cache` 约 `32M`，`/root/model_archive` 为 `0`。
- 已补齐 ST-Bench 训练数据：
  - `data/ST-Bench/ST-Align/alignment_train.jsonl`
  - `data/ST-Bench/ST-CoT/*.jsonl`
  - `data/ST-Bench/ST-RL/*.jsonl`
  - `data/ST-Bench/ST-Test/*.jsonl`
  - 数据目录约 `3.8G`。
- 已通过环境检查：
  - GPU：单张 A100-PCIE-40GB，检查时空闲。
  - Python：`/root/autodl-tmp/conda/envs/str-py310/bin/python`
  - 模型分片、tokenizer、TS config 均存在。
  - 训练所需 ST-Bench jsonl 均存在。
- 已启动过一次 Stage 1 10-step smoke：
  - 命令脚本：`00_new_codes/repro_autodl/experiments/scripts/single_a100_qwen3_4b_stage1_smoke.sh`
  - 日志：`00_new_codes/repro_autodl/experiments/logs/single_a100_qwen3_4b_stage1_smoke_20260604_103300.log`
  - 结果：失败，但不是显存爆、不是数据缺失、不是模型缺失。
  - 失败位置：DeepSpeed / Accelerate 准备 optimizer 时导入 `bitsandbytes`。
  - 关键错误：`ModuleNotFoundError: No module named 'triton.ops'`
  - 训练状态：尚未进入实际 backward/update step，也没有保存 checkpoint。
  - 输出目录：`output/single_a100_qwen3_4b_stage1_smoke`，当前大小为 `0`。
- 后续继续修复并重试 Stage 1 smoke：
  - 将当前环境中的 `bitsandbytes` 从 `0.43.1` 升级到 `0.45.2`，解决 `triton.ops` 导入失败；未改 torch/triton/deepspeed/transformers。
  - 在 Stage 1/2/3 smoke 脚本中加入 conda env `bin` 到 `PATH`，使 `ninja` 可执行文件可见。
  - 在 Stage 1/2/3 smoke 脚本中加入 `TORCH_EXTENSIONS_DIR=/root/autodl-tmp/cache/torch_extensions`，避免 PyTorch extension 写入系统盘。
  - DeepSpeed `CPUAdam` 已成功编译并加载，缓存目录约 `43M`。
  - 将 Stage 1/2 smoke 的 `DEEPSPEED_TIMEOUT` 从 `120` 调整为 `1800`，但后续证明 timeout 不是主因。
  - 最新失败位置：`Before initializing optimizer states` 之后子进程被 `SIGKILL`，返回码 `-9`。
  - 关键判断：cgroup `memory.max` 约 `77.3GB`，DeepSpeed 日志显示 CPU virtual memory 到约 `78.48GB`，4B full fine-tuning 的 optimizer states 初始化超过当前容器可用内存上限。
  - 训练状态：仍未进入实际 backward/update step，也没有保存 checkpoint。

当前磁盘状态：

- 系统盘 `/`：约 `3.0G / 30G`，使用率约 `10%`。
- 数据盘 `/root/autodl-tmp`：约 `32G / 50G`，使用率约 `64%`，剩余约 `19G`。
- 主要占用：
  - 4B 模型：约 `8.3G`
  - ST-Bench 数据：约 `3.8G`
  - HuggingFace cache：约 `5.3G`
  - `/root/.cache`：约 `32M`
  - `output/`：当前约 `0`

## 相关命令与脚本

如需重新校验 4B 模型完整性：

```bash
/root/autodl-tmp/conda/envs/str-py310/bin/python \
  00_new_codes/repro_autodl/experiments/scripts/single_a100_qwen3_4b_model_check.py
```

如需重新跑环境检查：

```bash
bash 00_new_codes/repro_autodl/experiments/scripts/single_a100_qwen3_4b_env_check.sh
```

已补充后续可用脚本：

- `00_new_codes/repro_autodl/experiments/scripts/single_a100_download_stbench_train_data.sh`
  - 用 `download_dataset.py` 补齐 ST-Bench。
  - 显式设置 cache 到 `/root/autodl-tmp/cache/huggingface`。
  - 下载前后打印 `/` 和 `/root/autodl-tmp` 磁盘状态。
- `00_new_codes/repro_autodl/experiments/scripts/single_a100_prepare_qwen3_4b_ts.sh`
  - 检查 4B safetensors 分片完整，兼容下载态和初始化保存后的分片数量。
  - 复制 `Config-Qwen3-4B-Instruct-2507`。
  - 调用 `initial_model.py` 初始化 TS encoder。
- `00_new_codes/repro_autodl/experiments/scripts/single_a100_qwen3_4b_model_check.py`
  - 按 `model.safetensors.index.json` 检查 4B 模型 safetensors 分片、index、tokenizer 是否完整。
  - 检查是否还有 `.incomplete` / `.lock` 断点文件。

依赖检查：

- `/root/autodl-tmp/conda/envs/str-py310` 中已有 `torch`、`transformers`、`deepspeed`、`vllm`、`huggingface_hub`、`datasets`、`accelerate`。
- `deepspeed` 不在默认 shell `PATH`，已把 Stage 1/2 smoke 脚本改成 `/root/autodl-tmp/conda/envs/str-py310/bin/deepspeed` 绝对路径。
- 4B 初始化后发现 `initial_model.py` 默认保存成 fp32，模型目录膨胀到约 `17G`；已修正 `initial_model.py`，显式 `torch_dtype=torch.float16` 加载并在保存前 `model.half()`。

## 本次过程日志

- 23:27 左右：第一次开始下载 `Qwen/Qwen3-4B-Instruct-2507`，发现系统盘 `/` 仍为 `93%`，用户提醒应先解决系统盘问题。
- 23:34 左右：停止 4B 下载，排查系统盘。确认 `/root/model_archive` 占约 `26G`，其中包含 `STReasoner-8B` 和 `STReasoner-8B-CoT` 两套模型副本。
- 23:36 左右：删除 `/root/model_archive/STReasoner-8B/` 和 `/root/model_archive/STReasoner-8B-CoT/`，系统盘从 `93%` 降到 `7%`。
- 23:38 左右：恢复 4B 下载，强制 `HF_HOME`、`TRANSFORMERS_CACHE`、`HF_HUB_CACHE` 等全部指向 `/root/autodl-tmp/cache/huggingface`。
- 23:43 左右：确认系统盘仍为 `7%`，数据盘增长，下载未写入 `/root/.cache` 或 `/root/model_archive`。
- 23:58 左右：`model-00002-of-00003.safetensors` 完成，`model-00001-of-00003.safetensors` 仍为 incomplete。
- 00:31 左右：最后一个 incomplete 分片约 `3.0G` 长时间无增长，停止下载并记录当时断点。
- 00:32 左右：用户要求继续下载；恢复下载。
- 00:56 左右：发现普通 xet 下载反复卡住，切换为 `HF_HUB_DISABLE_XET=1` 继续下载，最后分片从 `896M` 快速推进。
- 01:06 左右：4B 模型下载完成，完整性检查通过，无 incomplete / lock 文件。
- 01:08 左右：运行 4B TS 初始化，模型成功加载并保存；发现保存成 fp32，目录约 `17G`。
- 01:10 左右：修正 `initial_model.py`，显式 `torch_dtype=torch.float16` 加载并 `model.half()` 后保存。
- 01:12 左右：重新初始化保存完成；最终 2 个 fp16 shard，目录约 `8.3G`，GPU 空闲，系统盘保持 `7%`。
- 2026-06-04 收尾：按“没做的不开始”要求停止推进新任务；确认无下载、训练、vLLM、EasyR1 残留进程。`single_a100_qwen3_4b_model_check.py` 通过，`initial_model.py` 与检查脚本 py_compile 通过，`single_a100_*.sh` 语法检查通过。系统盘约 `10%`，数据盘约 `46%`，4B 模型目录约 `8.3G`。
- 2026-06-04 10:29 左右：重新运行 Stage 0 环境检查，确认 4B 模型完整，但训练数据缺 `ST-Align` / `ST-CoT` / `ST-RL`。
- 2026-06-04 10:29-10:32：运行 `single_a100_download_stbench_train_data.sh`，下载 ST-Bench 38 个文件到 `data/ST-Bench`，下载耗时约 `2m31s`。系统盘保持约 `10%`，数据盘升至约 `53%`。
- 2026-06-04 10:32：重新运行环境检查，确认 `ST-Align`、`ST-CoT`、`ST-RL`、`ST-Test` 训练/测试 jsonl 均存在；4B 模型完整性检查通过。
- 2026-06-04 10:33：启动 Stage 1 10-step smoke。数据读取和 tokenizer 预处理成功，alignment 训练集为 `153700` 条。
- 2026-06-04 10:36：模型权重加载成功，`Qwen3TSForCausalLM` 参数约 `4.44B`，ZeRO-3 初始化成功；随后在 optimizer 初始化阶段导入 `bitsandbytes` 失败，错误为 `ModuleNotFoundError: No module named 'triton.ops'`。
- 2026-06-04 10:36 后：确认没有训练进程残留，`output/single_a100_qwen3_4b_stage1_smoke` 大小为 `0`，没有生成 checkpoint。当前系统盘约 `10%`，数据盘约 `64%`。
- 2026-06-04 11:25 左右：将 `bitsandbytes` 升级到 `0.45.2`，轻量导入 `bitsandbytes` 和 `bitsandbytes.optim` 成功。
- 2026-06-04 11:26 左右：重跑 Stage 1 smoke，越过 `bitsandbytes/triton.ops`，但 DeepSpeed CPUAdam 编译时报 `RuntimeError: Ninja is required to load C++ extensions`。
- 2026-06-04 11:27 左右：确认 `ninja` 可执行文件存在于 conda env，但原脚本 PATH 不包含 env bin；修改 Stage 1/2/3 smoke 脚本加入 PATH 和 `TORCH_EXTENSIONS_DIR`。
- 2026-06-04 11:28 左右：重跑 Stage 1 smoke，CPUAdam 成功编译到 `/root/autodl-tmp/cache/torch_extensions`，但 optimizer states 初始化前后进程 `-9` 退出。
- 2026-06-04 11:30 左右：将 `DEEPSPEED_TIMEOUT` 提到 `1800` 后再次重跑，仍在 `Before initializing optimizer states` 后被 `-9` 杀掉，确认 timeout 不是主因。
- 2026-06-04 11:32 左右：确认 cgroup `memory.max` 约 `77.3GB`，DeepSpeed 日志中 CPU virtual memory 到约 `78.48GB`。当前结论：4B full fine-tuning + ZeRO-3 CPU offload 不适合当前容器内存上限。

## Q&A

启动过多次 **Stage 1 10-step smoke**。依赖问题已经逐步解决，但 full fine-tuning 版本仍没有真正跑到训练 step，最终卡在 optimizer states 初始化的容器内存上限。现在我已确认 **没有训练进程残留**，也不会继续重复 full smoke。

### 模型是否准备好了？

4B 模型本身是准备好的：

- 路径：`/root/autodl-tmp/STReasoner_reproduce/base_model/Qwen3-4B-Instruct-2507`
- 大小：`8.3G`
- 两个 safetensors shard 都在：`model-00001-of-00002.safetensors`、`model-00002-of-00002.safetensors`
- `config.json`、`modeling_qwen3_ts.py`、`processing_qwen3_ts.py`、tokenizer 等都完整
- 没有 `.incomplete` 或 lock 文件

### 训练是否准备好了？

full fine-tuning 路线没有。

依赖问题已处理；当前问题是 4B full fine-tuning 的 optimizer states 初始化超过当前容器内存上限。单卡可行路线需要改成 LoRA / QLoRA / 只训 TS encoder，或者换更小模型。

## 磁盘分工

磁盘分工现在是：

- 系统盘 `/`：30G，总使用 `3.0G / 30G`，约 `10%`
  - 只应该放系统、少量配置、临时小文件。
  - 不应该放模型、数据集、大 checkpoint、HF cache。
- 数据盘 `/root/autodl-tmp`：50G，总使用 `32G / 50G`，约 `64%`
  - 放代码、模型、数据、cache、训练输出。
  - 当前主要占用：
    - 4B 模型：`8.3G`
    - ST-Bench 数据：`3.8G`
    - HF cache：`5.3G`
    - Stage1 smoke 输出：`0`

一般建议：

- 系统盘 `/`：最好低于 `50%`，超过 `70%` 就要警惕，超过 `85%` 很危险。
- 数据盘 `/root/autodl-tmp`：普通实验可到 `70%-80%`，但训练要保存 checkpoint 时最好至少留 `15-20G`。
- 现在数据盘剩 `19G`，**能做 smoke 和小步数训练，但不适合随便保存多个完整 checkpoint**。后续要么限制 checkpoint 数量，要么只保存必要断点，要么训练前再清一轮 cache/旧输出。
