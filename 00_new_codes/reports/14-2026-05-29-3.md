# 2026-05-29 日志 3

13:00：接续前一轮任务，未重复启动 entity smoke，而是继续轮询已有 vLLM 会话。

13:01：`reasoning_entity` 的 ST-Test 1 条 smoke 完成，输出保存到 `exp/sttest_smoke_entity_1/generated_answer.json`。模型答 `D`，对应 gold 为 `A`，官方 evaluate 结果 accuracy 为 `0.0`。

13:02：第一次直接执行 `evaluation/evaluate.py` 时出现 `ModuleNotFoundError: No module named 'evaluation'`，原因是从文件路径执行时项目根目录未进入 `PYTHONPATH`。改用 `PYTHONPATH=.` 后评估通过。

13:03：`reasoning_etiological` 的 ST-Test 1 条 smoke 完成，输出保存到 `exp/sttest_smoke_etiological_1/generated_answer.json`。模型答 `B`，gold 为 `B`，官方 evaluate 结果 accuracy 为 `1.0`。

13:06：`reasoning_correlation` 的 ST-Test 1 条 smoke 完成，输出保存到 `exp/sttest_smoke_correlation_1/generated_answer.json`。模型答 `D`，官方 evaluate 结果 accuracy 为 `0.0`。

13:08：`reasoning_forecasting` 的 ST-Test 1 条 smoke 完成，输出保存到 `exp/sttest_smoke_forecasting_1/generated_answer.json`。模型输出 `[32.5, 33.0, 36.5]`，官方 evaluate 结果 MAE 为 `29.75`，MAPE 为 `158.8235%`。

13:10：确认 ST-Test 四类数据均已下载到 `data/ST-Bench/ST-Test/`，共 3273 条：correlation 1592、entity 1194、etiological 207、forecasting 280。

13:12：把 ST-Test 四类 smoke 的命令、结果和限制追加到 `00_new_codes/reports/11_2.2paper_cases重试修复报告.md`。明确记录：这只是官方推理和评估链路 smoke，不是完整 ST-Test 论文效果复现。

13:15：完整运行 `reasoning_etiological` 的 ST-Test 207 条，输出到 `exp/sttest_full_etiological_512/generated_answer.json`。官方/当前 evaluate 结果：coverage `1.0`，accuracy `0.937198`。

13:23：完整运行 `reasoning_forecasting` 的 ST-Test 280 条，输出到 `exp/sttest_full_forecasting_512/generated_answer.json`。官方/当前 evaluate 结果：coverage `1.0`，MAE `67.430537`，MAPE `138.878661%`。

13:45：完整运行 `reasoning_entity` 的 ST-Test 1194 条，输出到 `exp/sttest_full_entity_512/generated_answer.json`。官方/当前 evaluate 结果：coverage `1.0`，accuracy `0.742044`。

14:33：完整运行 `reasoning_correlation` 的 ST-Test 1592 条，输出到 `exp/sttest_full_correlation_512/generated_answer.json`。官方/当前 evaluate 结果：coverage `1.0`，accuracy `0.862437`。

14:36：统计完整 ST-Test raw `<answer>` 格式：entity 1188/1194 严格合格，etiological 207/207，correlation 1566/1592，forecasting 280/280；没有空输出。

14:38：把完整 ST-Test 运行结果、输出目录、评估指标和格式诊断补入 `00_new_codes/reports/11_2.2paper_cases重试修复报告.md`。
