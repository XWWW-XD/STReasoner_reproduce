# 12-2026-05-29-1

- 01-02：确认现有 Stage 2.2 正式结果不完整，历史结果主要是 T4 OOM、time-series merge 长度不匹配、16-token smoke 截断。
- 01-03：运行 `baseline_current_6144`，发现原 `run-all` 每条样例重复加载 STReasoner-8B，第 3 条加载时 A100 40G OOM。
- 01-10：修改 `run_paper_cases.py`，让 `run-all` 只加载一次模型并复用，同时修复 `failure_type_from()` 参数数量错误。
- 01-12：运行 `fixed_reuse_model_6144`，4 条 paper_cases 全部生成完成。
- 01-55：修复 `evaluation/evaluate_qa.py` 的选择题和 forecasting 提取逻辑，并基于已有生成文本重解析到 `parserfix_reparse_existing_6144`。
- 01-56：修复后 3 道选择题全部正确，forecasting MAE 从 7.42 降到 0.1667，MAPE 从 37.33% 降到 0.8349%。

