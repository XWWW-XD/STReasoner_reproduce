# 2026-06-04-10 Stage 1 输出索引整理日志

## 本轮范围

本轮仍不启动训练，只整理 Stage 1 当前输出、日志、探针和下次继续清单。

## 已完成

新增：

```text
00_new_codes/reports/32-Stage1当前输出索引与继续训练清单.md
```

内容包括：

- `smoke / 100 / 500 / 1000 / 1500 / 2000 save_state` 六个输出目录索引。
- 每段的训练含义、`train_loss`、`train_runtime`、目录大小。
- 哪些 adapter 可标准 PEFT 加载。
- 哪些 checkpoint 可严格 `resume_from_checkpoint`。
- 6条、30条、40条探针结果索引。
- 关键日志和结果文件路径。
- 下次运行 `2000 -> 2500` 前的检查清单。
- 跑完后必须做的验证步骤。

## 关键结论

当前最新可靠断点仍是：

```text
output/single_a100_qwen3_4b_stage1_lora_2000steps_from1500_save_state/checkpoint-500
```

后续训练脚本是：

```text
00_new_codes/repro_autodl/experiments/scripts/single_a100_qwen3_4b_stage1_lora_resume_2000to2500_save_state.sh
```

该脚本应输出到：

```text
output/single_a100_qwen3_4b_stage1_lora_2500steps_from2000_save_state/
```

本轮没有启动训练。
